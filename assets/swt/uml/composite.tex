\documentclass{article}
\usepackage[utf8]{inputenc} % use utf8 file encoding for TeX sources
\usepackage[T1]{fontenc}    % avoid garbled Unicode text in pdf
\usepackage{tikz}
\usepackage{tikz-uml}
\usepackage{amssymb}
\usetikzlibrary{automata, positioning, arrows}

\newcommand{\code}{\texttt}

\begin{document}
\pagestyle{empty}
\begin{figure}[ht]
\centering

\begin{tikzpicture}

  \tikzumlset{
    fill class=none,
    fill note=none
  }

  \umlclass[type=abstract]{Node}{}{
    \umlvirt{gemeinsameOperation()} \\
    add(:Node) \\
    remove(:Node) \\
    get(index)
  }
  \umlclass[below left=1cm and 0cm of Node]{Leaf}{}{
    gemeinsameOperation() \\ \\ \\ \\
  }
  \umlclass[below right=1cm and 0cm of Node]{Composite}{}{
    gemeinsameOperation() \\
    add(:Node) \\
    remove(:Node) \\
    get(index)
  }

  \umlinherit[geometry=|-|]{Leaf}{Node}
  \umlinherit[geometry=|-|]{Composite}{Node}

  \umluniaggreg[geometry=|-,anchors=50 and 0]{Composite}{Node}

  \umlnote[width=3.6cm,left=2cm of Node]{Node}{NOP Impementationen von \code{add}, \code{remove} und \code{get}}

\end{tikzpicture}
\end{figure}
\end{document}
